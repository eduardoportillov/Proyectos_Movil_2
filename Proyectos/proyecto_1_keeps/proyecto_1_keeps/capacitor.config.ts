import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_1_keeps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
