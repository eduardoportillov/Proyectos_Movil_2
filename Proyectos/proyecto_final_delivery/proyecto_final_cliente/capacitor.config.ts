import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_final_cliente',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
