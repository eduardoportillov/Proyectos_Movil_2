import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Practica_Calculadora',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
